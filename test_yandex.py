# coding=utf-8
# Generated by Selenium IDE
from Barancev_Test_QA.group_auth import Group
from Barancev_Test_QA.application import Application
import pytest

@pytest.fixture()
def app():
    fixture = Application()
    return fixture

class TestYandex():

    def test_1_delete_group(self, app):
        app.open_site()
        app.login(Group(username='poterok', password='Mnata1991'))
        app.go_to_contacts()
        app.go_to_new_group()
        app.go_to_settings_group()
        app.choose_group_for_delete()
        app.delete_group()
        app.open_site()
        app.logout()


    def test_2_create_new_group(self,app):
        app.login(Group(username='poterok', password='Mnata1991'))
        app.go_to_contacts()
        app.create_new_group()
        app.open_site()
        app.logout()

    def test_3_login_logout(self, app):
        app.open_site()
        app.login(Group(username='poterok', password='Mnata1991'))
        app.logout()

    def teardown_method(self):
        app.destroy()
